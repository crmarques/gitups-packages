apiVersion: v1
kind: ConfigMap
metadata:
  name: gitea-env-config
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/part-of: support-services
data:
  profile: "{{ .Env }}"
  root-url: "{{ .Values.rootURL }}"

