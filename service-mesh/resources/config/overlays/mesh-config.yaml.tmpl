apiVersion: v1
kind: ConfigMap
metadata:
  name: mesh-env-config
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: service-mesh
    app.kubernetes.io/part-of: support-services
data:
  profile: "{{ .Env }}"
  mesh-id: "{{ .Values.meshID }}"

