apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: {{ .Values.poolName }}
  namespace: {{ .Values.namespace }}
spec:
  addresses:
{{- range .Values.cidrs }}
    - {{ . }}
{{- end }}
